# Travel Checker

## Installation

```
$> sudo dnf install php
$> sudo dnf install php-mysqli
$> sudo dnf install sql
```

## Database Installation

Le fichier .sql se trouve à l'interieur du fichier Database, il faut import ce fichier sur phpmyadmin.

## Run mobile app
Demarrer un serveur en Localhost.

```
$> php -S localhost:8000
```

le serveur sera accessible depuis http://localhost:8000
